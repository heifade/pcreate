import {  } from "../src/index";
import { expect } from "chai";
import "mocha";

describe("Delete", function() {
  
  before(async () => {
    
  });
  after(async () => {
  });

  it("delete must be success", async () => {
  });
});