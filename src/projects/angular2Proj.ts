
import { BaseProj } from './baseProj';
import { Questions } from 'inquirer';

export class Angular2Proj extends BaseProj {
  getQuestions() {
    let questionList: Questions[] = [
    ];

    return questionList;
  }
  async run () {
    
  }
}