import { expect } from "chai";
import "mocha";

describe("test", function() {
  before(async () => {});
  after(async () => {});

  it("test should be success", async () => {
    expect(1).to.equal(1);
  });
});
